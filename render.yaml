services:
  - type: web
    name: zubairs-gpt
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn server:app
    envVars:
      - key: OPENROUTER_API_KEY
        sync: false
